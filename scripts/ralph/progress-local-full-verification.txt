## Codebase Patterns
- Local-only verification must complete before any VPS deployment to 212.227.227.64.
- A Ralph story is complete only when its acceptance criteria have command, SQL, log, or test evidence.
- Empty compliance tables are not automatically acceptable; classify each as populated, allowed-empty, or blocker with evidence.

## 2026-05-10 - LOCAL-000
- Created a separate local-only Ralph backlog so existing VPS-focused `scripts/ralph/prd.json` is not overwritten.
- Sources for methodology: https://github.com/snarktank/ralph README; key loop is one small story per iteration, quality checks, mark `passes`, append progress.
- **Learnings for future iterations:**
  - Do not deploy to VPS in this phase. The user explicitly said the VPS at 212.227.227.64 is not accessible yet and all checks are local first.
  - Keep evidence append-only in this file; do not replace previous results.
---

## 2026-05-10 - TABLE-005
- Completed document decomposition remediation.
- Created `apps/workers/document_decomposition.py` to derive sections, fragments, CNMV version rows, and lineage from existing official `documento_interpretativo` rows.
- Created `apps/workers/tests/test_document_decomposition.py`.
- Populated:
  - `documento_seccion`: 94 rows
  - `documento_fragmento`: 4,749 rows
  - `documento_cnmv_version`: 72 rows
  - `data_lineage`: 4,915 rows
- Evidence:
  - `rtk powershell ... pytest apps/workers/tests/test_document_decomposition.py -q` => `2 passed`.
  - `docker compose run --rm worker-boe python document_decomposition.py --run-once` => `{'documents': 94, 'sections': 94, 'fragments': 4749, 'cnmv_versions': 72}`.
  - SQL count summary confirms populated target tables.
  - Gate now reports `blockers_empty: 68`, down from `72`.
- Updated `scripts/ralph/prd-local-table-remediation.json`: `TABLE-005` now has `passes: true`.
- **Learnings for future iterations:**
  - Derived retrieval chunks must remain linked to `documento_interpretativo.url_fuente`; chunks are not independent legal authority.
  - `data_lineage` is now useful as the trace layer for derived document artifacts.
---

## 2026-05-10 - TABLE-004
- Completed PGC official BOE ingestion for the core PGC tables.
- Created `apps/workers/pgc_boe.py` to fetch and parse official BOE `BOE-A-2007-19884` instead of using static/corrupted seed data.
- Created `apps/workers/tests/test_pgc_boe.py` for account, marco, and NRV parsing.
- Populated:
  - `pgc_marco`: 6 rows
  - `pgc_cuenta`: 882 rows
  - `pgc_norma_valoracion`: 23 rows
  - `pgc_estado_financiero`: 3 rows
- Reclassified `pgc_cuenta_fiscal_ref` and `pgc_cuenta_modelo_aeat_ref` from `derived_blocker` to `workflow_empty` because they are interpretative/human-reviewed mapping tables. They must not be inferred automatically just to satisfy row counts.
- Evidence:
  - `docker compose run --rm worker-boe python pgc_boe.py --run-once` => `PGC BOE sync complete: {'marcos': 6, 'cuentas': 882, 'normas': 23, 'estados': 0}` on idempotent rerun after initial 3 estados inserted.
  - SQL count summary shows `pgc_cuenta=882`, `pgc_marco=6`, `pgc_norma_valoracion=23`, `pgc_estado_financiero=3`.
  - Sample SQL confirms account `100 = Capital social` and `700 = Ventas de mercaderias` sourced from `BOE-A-2007-19884`.
  - `rtk powershell ... pytest apps/workers/tests/test_pgc_boe.py apps/workers/tests/test_aeat_irnr.py -q` => `26 passed`.
  - Gate now reports `blockers_empty: 72`, down from `78`.
- Updated `scripts/ralph/prd-local-table-remediation.json`: `TABLE-004` now has `passes: true`.
- **Learnings for future iterations:**
  - `apps/workers/pgc_real.py` and `pgc_dataset.py` should not be treated as authoritative; the BOE parser is the safer path.
  - Mapping PGC accounts to AEAT model/casilla fields is a derived interpretation and must require human review or official mapping evidence.
---

## 2026-05-10 - TABLE-003 completion
- Completed IRNR table remediation for local gate purposes.
- Extended `apps/workers/aeat_irnr.py` to fetch official AEAT IRNR rates page:
  - `https://sede.agenciatributaria.gob.es/Sede/no-residentes/irnr-sin-establecimiento-permanente/tipos-gravamen-irnr-sin-establecimiento-permanente.html`
  - Rows are linked to BOE legal basis `BOE-A-2004-4527`, Article 25 TRLIRNR.
- Populated `irnr_withholding_rate` with 15 official-source rows for model 210.
- Important accuracy note:
  - The AEAT page provides exact rates and page update date (`18/junio/2025`), but not an individual legal effective date for every rate row.
  - Rows without explicit per-row vigencia keep `effective_date = NULL` and include `uncertainty_notes`. This avoids fabricating dates.
- Evidence:
  - `docker compose run --rm worker-boe python aeat_irnr.py --run-once` => `IRNR sync complete: 7 upserted, 7 instructions, 15 rates, 0 skipped, 0 deprecated`.
  - SQL: `select count(*) from irnr_withholding_rate` => `15`.
  - SQL: all 15 rows linked to model `210`, `source_family = 'AEAT official portal; BOE official text'`.
  - `rtk powershell ... pytest apps/workers/tests/test_aeat_irnr.py apps/workers/tests/test_boe_modelos_worker.py apps/api/tests/test_source_manifest.py scripts/tests/test_maintenance_agents.py -q` => `34 passed`.
  - `python scripts\ralph\table_registry.py --gate scripts\ralph\table-remediation-registry.json` now reports `blockers_empty: 78`.
- Updated `scripts/ralph/prd-local-table-remediation.json`: `TABLE-003` now has `passes: true`.
- **Learnings for future iterations:**
  - In legal/tax data, NULL plus explicit uncertainty is safer than inventing effective dates.
  - Later API/MCP response code must surface `uncertainty_notes` for these rows if asked about vigencia.
---

## 2026-05-10 - TABLE-003 partial
- Added migration `alembic/versions/20260510_0065_irnr_provenance.py` for IRNR provenance columns:
  - `irnr_instruccion.source_url`
  - `irnr_instruccion.source_family`
  - `irnr_withholding_rate.source_url`
  - `irnr_withholding_rate.source_family`
  - `irnr_withholding_rate.effective_date`
  - `irnr_withholding_rate.legal_basis`
  - `irnr_withholding_rate.uncertainty_notes`
- Applied equivalent local Postgres `ALTER TABLE ... ADD COLUMN IF NOT EXISTS ...` statements and advanced local `alembic_version` to `20260510_0065_irnr_provenance`.
- Extended `apps/workers/aeat_irnr.py` to fetch official AEAT model pages and upsert `irnr_instruccion` rows with source provenance.
- Added focused tests for IRNR official page instruction extraction and instruction upsert.
- Evidence commands:
  - `rtk powershell ... pytest apps/workers/tests/test_aeat_irnr.py -q` => `21 passed`.
  - `docker compose build worker-boe` rebuilt the worker image after host edits.
  - `docker compose run --rm worker-boe python aeat_irnr.py --run-once` => `IRNR sync complete: 7 upserted, 7 instructions, 0 skipped, 0 deprecated`.
  - SQL: `select count(*) from irnr_instruccion` => `7`.
  - SQL rows show `source_family = 'AEAT official portal'` and source URLs under `https://sede.agenciatributaria.gob.es/Sede/procedimientoini/...`.
  - `python scripts\ralph\table_registry.py --gate scripts\ralph\table-remediation-registry.json` now reports `blockers_empty: 79`, down from `80`.
- Remaining TABLE-003 blocker:
  - `irnr_withholding_rate` remains empty. Do not populate it from `seed_irnr_rates.py`; it needs exact AEAT/BOE official rates with legal basis, effective dates, and uncertainty notes.
- **Learnings for future iterations:**
  - The current AEAT IRNR index does not list model 296, but the broader AEAT model worker already has model 296 from the AEAT procedures area. Treat 296 separately when implementing rate/legal-basis ingestion.
  - Running SQL in parallel with a worker can observe pre-run counts; re-query after the worker exits for evidence.
---

## 2026-05-10 - TABLE-002
- Completed the local table gate command.
- Extended `scripts/ralph/table_registry.py` with:
  - `--write` to generate `table-remediation-registry.json`.
  - `--verify-json` to verify the registry against live local Postgres.
  - `--gate` to fail deployment when any `blocker` or `derived_blocker` table is empty.
  - RLS checks for all registered tables.
  - URL provenance allowlist checks for URL-like columns in non-workflow/non-operational tables.
- Evidence commands:
  - `python scripts\ralph\table_registry.py --write scripts\ralph\table-remediation-registry.json` => `163,45,67,13,35,3,0`
  - `python scripts\ralph\table_registry.py --verify-json scripts\ralph\table-remediation-registry.json` => `OK: verified scripts\ralph\table-remediation-registry.json`
  - `python scripts\ralph\table_registry.py --gate scripts\ralph\table-remediation-registry.json` => exit code `2`, expected at this stage.
- Current gate result:
  - total_tables: 163
  - blockers_empty: 80
  - errors: 80
- Updated `scripts/ralph/prd-local-table-remediation.json`: `TABLE-002` now has `passes: true`.
- **Learnings for future iterations:**
  - The gate is now doing its job: it blocks VPS deployment until required empty tables are remediated or reclassified with evidence.
  - Next Ralph story is `TABLE-003` for IRNR because it is AEAT/BOE tax-domain data and higher compliance risk than broader financial-domain target-state tables.
---

## 2026-05-10 - LOCAL-002
- Completed local database table verification using the Ralph table remediation registry.
- Final table registry summary:
  - total_tables: 163
  - populated: 69
  - blocker: 0
  - derived_blocker: 0
  - workflow_empty: 91
  - allowed_empty: 3
  - unclassified: 0
- Evidence commands:
  - `python scripts\ralph\table_registry.py --verify-json scripts\ralph\table-remediation-registry.json` => `OK`.
  - `python scripts\ralph\table_registry.py --gate scripts\ralph\table-remediation-registry.json` => `OK`, `blockers_empty=0`, `errors=0`.
- Updated `scripts/ralph/prd-local-full-verification.json`: `LOCAL-002` now has `passes: true`.
- Remaining caveat:
  - This is a table-gate pass, not a full production-readiness verdict. Several tables are intentionally `workflow_empty` because no official upstream parser or user workflow has produced real rows.
---

## 2026-05-10 - TABLE-010 / fiscal-normative closure
- Completed final local table gate package after an extra fiscal/normative closure pass.
- Extended `apps/workers/official_regulatory_references.py` with:
  - IRS reference metadata: `irs_fiscal_norma=2`, `irs_modelo=4`, `irs_w8_form=4`, `irs_tin_reference=2`, `irs_withholding_rule=1`.
  - AEAT model references: `modelo_formato=1`, `obligacion_regulatoria=12`.
- Rejected GIIN seed fallback:
  - `docker compose run --rm worker-boe python giin.py --run-once` returned `15 entries from seed`.
  - Deleted those 15 local rows from `giin_registry`.
  - Reclassified `giin_registry` as `workflow_empty` until official IRS GIIN CSV fetch succeeds.
- Reclassified empty high-risk tables as safe-empty only where serving authoritative facts would be unsafe without a dedicated official source:
  - `modelo_fiscal_calendar`: empty until current AEAT calendar ingestion exists.
  - `obligacion_documento`, `obligacion_micro_obligacion`, `obligacion_internacional`: empty until exact official source linking/decomposition is implemented.
- Fixed SQLite compatibility in `apps/api/services/modelos.py`: `list_modelos_summary` now returns `NULL AS url_listado` when the test schema lacks `aeat_modelo.url_listado`.
- Evidence commands:
  - `docker compose run --rm worker-boe python official_regulatory_references.py --run-once` => `{'csrd_ess': 12, 'dora_framework': 1, 'sepa_rules': 2, 'pbc_obligated_subjects': 6, 'screening_lists': 2, 'irs_fiscal_norma': 2, 'irs_modelo': 4, 'irs_w8_form': 4, 'irs_tin_reference': 2, 'irs_withholding_rule': 1, 'modelo_formato': 1, 'obligacion_regulatoria': 12}`.
  - `python scripts\ralph\table_registry.py --write scripts\ralph\table-remediation-registry.json` => `163,69,0,0,91,3,0`.
  - `python scripts\ralph\table_registry.py --gate scripts\ralph\table-remediation-registry.json` => `OK`, `blockers_empty=0`, `errors=0`.
  - `$env:PYTHONPATH='apps;apps/api;apps/workers'; python -m pytest apps/api/tests/test_modelos_truth_contract.py apps/api/tests/test_irs_fiscal.py apps/api/tests/test_irs_w8.py apps/api/tests/test_irs_modelos.py apps/api/tests/test_obligaciones_metadata.py apps/api/tests/test_micro_obligaciones.py apps/workers/tests/test_official_regulatory_references.py -q` => `119 passed, 4 warnings`.
- Updated `scripts/ralph/prd-local-table-remediation.json`: `TABLE-010` now has `passes: true`.
- Final local table-gate decision:
  - `CONDITIONAL PASS` for local table registry/gate only.
  - Not `PRODUCTION READY` for the full legal/tax MCP, because several critical fact domains are intentionally `workflow_empty`/not implemented rather than fully populated from official sources.
---

## 2026-05-10 - TABLE-009
- Completed ownership, PBC, fraud, and screening remediation/classification pass.
- Extended `apps/workers/official_regulatory_references.py` with:
  - `pbc_obligated_subject`: 6 official Ley 10/2010 article 2 subject-category metadata rows from BOE.
  - `screening_lists`: 2 official list metadata rows for EU sanctions resources and SEPBLAC.
- Ran `entity_identity.py --run-once` against local companies:
  - `empresas_procesadas=12`
  - `leis_encontrados=0`
  - `aliases_creados=0`
  - Result: `entity_identifiers` and `entity_aliases` stay `workflow_empty`; no fake LEIs or aliases inserted.
- Reclassified entity-specific or user/source-specific tables as `workflow_empty`: `beneficial_owner_record`, `ownership_relation`, `ownership_share`, `ubo_record`, `pbc_entity`, and `screening_entries`.
- Adjusted `apps/api/tests/test_pbc.py` so suspicious activity report list tests accept the safe operational-empty response instead of requiring fake SAR rows.
- Evidence commands:
  - `docker compose run --rm worker-boe python official_regulatory_references.py --run-once` => `{'csrd_ess': 12, 'dora_framework': 1, 'sepa_rules': 2, 'pbc_obligated_subjects': 6, 'screening_lists': 2}`.
  - `docker compose run --rm worker-boe python entity_identity.py --run-once` => `{'status': 'ok', 'empresas_procesadas': 12, 'leis_encontrados': 0, 'aliases_creados': 0}`.
  - SQL counts: `pbc_obligated_subject=6`, `screening_lists=2`, `entity_identifiers=0`, `entity_aliases=0`.
  - `$env:PYTHONPATH='apps;apps/api;apps/workers'; python -m pytest apps/api/tests/test_pbc.py apps/api/tests/test_screening.py apps/api/tests/test_ownership.py apps/api/tests/test_entity_identity.py apps/workers/tests/test_official_regulatory_references.py -q` => `124 passed, 4 warnings`.
  - `python scripts\ralph\table_registry.py --gate scripts\ralph\table-remediation-registry.json` => exit code `2`, expected while 12 blocker/derived-blocker tables remain empty.
- Updated `scripts/ralph/prd-local-table-remediation.json`: `TABLE-009` now has `passes: true`.
- **Learnings for future iterations:**
  - The repo has historical seed scripts for ownership/PBC/screening, but they are not acceptable deployment evidence under the official-source rule.
  - Remaining blockers are now limited to IRS/international tax, AEAT model metadata, and obligations graph tables.
---

## 2026-05-10 - TABLE-008
- Completed financial regulatory domain remediation/classification pass.
- Added `apps/workers/official_regulatory_references.py` to populate only compact official-reference catalogs:
  - `csrd_ess`: 12 ESRS reference rows from EUR-Lex Delegated Regulation (EU) 2023/2772.
  - `dora_incident_classification_framework`: 1 DORA RTS reference row from EUR-Lex Delegated Regulations (EU) 2024/1772 and 2025/301.
  - `sepa_payment_rule`: 2 SEPA reference rows traced to EUR-Lex Regulations (EU) 260/2012 and 2024/886.
- Added `apps/workers/tests/test_official_regulatory_references.py`.
- Reclassified entity-, fund-, product-, filing-, client-, incident-, and surveillance-output financial tables as `workflow_empty` where no real official registry/filing upstream is configured locally. No sample rows from `scripts/data/seed_*` were accepted as production evidence.
- Adjusted `apps/api/tests/test_psd2.py` so the PSD2 incident endpoint accepts a healthy empty incident table instead of requiring a fake incident fixture.
- Evidence commands:
  - `docker compose build worker-boe` => rebuilt worker image with the new worker.
  - `docker compose run --rm worker-boe python official_regulatory_references.py --run-once` => `{'csrd_ess': 12, 'dora_framework': 1, 'sepa_rules': 2}`.
  - SQL counts: `csrd_ess=12`, `dora_incident_classification_framework=1`, `sepa_payment_rule=2`, `source_revision` rows for worker `official-regulatory-references=15`.
  - `$env:PYTHONPATH='apps;apps/api;apps/workers'; python -m pytest apps/api/tests/test_psd2.py apps/api/tests/test_csrd.py apps/api/tests/test_dora.py apps/workers/tests/test_official_regulatory_references.py -q` => `84 passed, 4 warnings`.
  - `python scripts\ralph\table_registry.py --gate scripts\ralph\table-remediation-registry.json` => exit code `2`, expected while 22 non-financial blocker/derived-blocker tables remain empty.
- Updated `scripts/ralph/prd-local-table-remediation.json`: `TABLE-008` now has `passes: true`.
- **Learnings for future iterations:**
  - Financial domain seed workers in this repo contain realistic fixtures, not authoritative production data; they remain rejected unless replaced with official source ingestion.
  - The remaining blockers are now concentrated in ownership/PBC/screening, IRS/international tax, fiscal model metadata, and obligations graph tables.
---

## 2026-05-10 - TABLE-007
- Completed classification and verification for operational event-only tables.
- Verified the following local operational tables are empty by design, not missing production data:
  - `ai_model_registry`: `allowed_empty`, RLS enabled.
  - `data_freshness_alerts`: `allowed_empty`, RLS enabled.
  - `embedding_version`: `allowed_empty`, RLS enabled.
  - `eval_run`: `workflow_empty`, RLS enabled.
  - `eval_query`: `workflow_empty`, RLS enabled.
  - `human_review`: `workflow_empty`, RLS enabled.
  - `nota_editorial_interna`: `workflow_empty`, RLS enabled.
- No fake alert, review, eval, model-registry, or editorial rows were inserted.
- Evidence commands:
  - Registry JSON inspection confirmed explicit classifications, actions, and `not_applicable_workflow_or_operational` source family for the tables above.
  - SQL RLS check returned `relrowsecurity = t` for all seven tables above, all with zero rows.
  - `$env:PYTHONPATH='apps;apps/api'; python -m pytest apps/api/tests/test_governance_http.py apps/api/tests/test_human_review.py apps/api/tests/test_query_audit_http.py apps/api/tests/test_source_manifest.py apps/workers/tests/test_change_detection.py -q` => `76 passed, 4 warnings`.
- Updated `scripts/ralph/prd-local-table-remediation.json`: `TABLE-007` now has `passes: true`.
- **Learnings for future iterations:**
  - Operational/event tables should not be force-filled to satisfy a row-count policy; the gate must verify schema, RLS, and behavior instead.
  - Historical seed scripts that inserted fake operational rows are not valid deployment evidence for this local audit.
---

## 2026-05-10 - LOCAL-003
- Completed local cron/worker run-once verification for all 15 cron-profile services discovered in `infra/deploy/docker-compose.prod.yml`.
- Added `scripts/ralph/verify_workers_local.py` to enumerate cron services, execute each `WORKER_CMD` in the local Docker worker container, enforce container-level timeouts, capture stdout/stderr evidence, and clean up verification containers.
- Rebuilt the local worker image after host-code changes with `docker compose build worker-boe`; the worker image copies source at build time, so rebuild is required for valid evidence.
- Fixed a real silent-failure bug in `apps/workers/regulatory_watch.py`: it attempted to write detected changes to non-existent/deprecated `regulatory_changes` while returning exit code 0. It now records idempotent regulatory changes in canonical `source_revision`.
- Added regression coverage in `apps/workers/tests/test_regulatory_watch.py`.
- Verification summary:
  - `cron-aepd-weekly`, `cron-bde-weekly`, `cron-bdns-weekly`, `cron-borme-weekly`, `cron-cendoj-weekly`, `cron-cnmv-weekly`, `cron-dgt-weekly`, `cron-eurlex-weekly`, `cron-sepblac-weekly`, `cron-teac-weekly`: 10/10 PASS in `scripts/ralph/worker-run-once-results-weekly.json`.
  - `cron-psd2-weekly`: PASS in `scripts/ralph/worker-run-once-results-psd2.json`; note: worker reports `source=bde_verified_seed`, which remains an accuracy/provenance review item even though execution succeeds.
  - `cron-boe-modelos-daily`: PASS in `scripts/ralph/worker-run-once-results-boe-modelos.json`.
  - `cron-regulatory-daily`: PASS after fix in `scripts/ralph/worker-run-once-results-regulatory-fixed.json`; remaining stderr contains upstream warnings for EUR-Lex/AEPD/BDE availability, but no DB table failure.
  - `cron-modelos-daily`: PASS in `scripts/ralph/worker-run-once-results-modelos.json`, duration 581.69s, `Sync complete: 217 upserted, 0 skipped, 0 deprecated`.
  - `cron-boe-daily`: PASS in `scripts/ralph/worker-run-once-results-boe-full.json`, duration 1262.47s, `[run-once] Bloques: 1127, Articulos: 1127`.
- Post-run DB evidence:
  - `articulo`: 3032 rows.
  - `version_articulo`: 3546 rows.
  - `sync_log`: latest `cron-boe-daily` row `status=ok`, `errors=0`; latest `cron-regulatory-daily` row `status=ok`, `rows_processed=1`, `errors=0`.
- Evidence commands:
  - `python scripts\ralph\verify_workers_local.py --service cron-boe-daily --timeout 1800 --mode dev-worker --output scripts\ralph\worker-run-once-results-boe-full.json` => PASS.
  - `python scripts\ralph\verify_workers_local.py --service cron-modelos-daily --timeout 900 --mode dev-worker --output scripts\ralph\worker-run-once-results-modelos.json` => PASS.
  - `python scripts\ralph\verify_workers_local.py --service cron-boe-modelos-daily --timeout 240 --mode dev-worker --output scripts\ralph\worker-run-once-results-boe-modelos.json` => PASS.
  - `python scripts\ralph\verify_workers_local.py --service cron-regulatory-daily --timeout 240 --mode dev-worker --output scripts\ralph\worker-run-once-results-regulatory-fixed.json` => PASS.
  - `$env:PYTHONPATH='apps;apps/api'; python -m pytest apps\workers\tests\test_regulatory_watch.py apps\workers\tests\test_change_detection.py -q` => `16 passed`.
  - `python scripts\ralph\table_registry.py --write scripts\ralph\table-remediation-registry.json; python scripts\ralph\table_registry.py --verify-json scripts\ralph\table-remediation-registry.json; python scripts\ralph\table_registry.py --gate scripts\ralph\table-remediation-registry.json` => `163,69,0,0,91,3,0` and gate PASS.
- Updated `scripts/ralph/prd-local-full-verification.json`: `LOCAL-003` now has `passes: true`.
- **Learnings for future iterations:**
  - Worker verification on this local compose must use detached containers and poll Docker state; direct `docker compose run` can outlive the Python timeout on Windows.
  - `cron-boe-daily` needs a long window for full evidence; a short run may only prove lock/concurrency behavior when the continuous BOE worker is active.
  - A cron returning exit code 0 is not enough; stderr and `sync_log` must be checked for hidden failures.
---

## 2026-05-10 - LOCAL-004 partial
- Started local scripts/tooling verification.
- Safe syntax gate passed:
  - `python -m compileall -q scripts` => exit code `0`.
- Script test suite result before remediation:
  - `$env:PYTHONPATH='apps;apps/api;apps/workers'; python -m pytest scripts\tests -q` => `2 failed, 386 passed, 4 warnings, 259 errors`.
  - The 259 errors are not accepted as product failures yet because the tests connect to host `localhost:5432`, which is currently owned by an external/local `postgres` process, not the Docker DB used by the stack. Evidence: `Get-NetTCPConnection -LocalPort 5432 -State Listen` reports owning process `postgres`; Docker runtime remains healthy via `docker compose exec postgres`.
  - This is a LOCAL-004 environment blocker for legacy DB-bound seed tests; do not run seed scripts to "fix" it because many are fixture/seed scripts rejected by the real-data rule.
- Real configuration failure found and fixed:
  - `scripts/tests/test_compose_env_example.py` failed because `infra/deploy/compose.env.example` still exposed `NEXT_PUBLIC_API_BASE_URL` and missed variables referenced by `infra/deploy/docker-compose.prod.yml`.
  - Updated `infra/deploy/compose.env.example` to remove `NEXT_PUBLIC_API_BASE_URL` and add the missing compose runtime keys.
  - `python -m pytest scripts\tests\test_compose_env_example.py -q` => `3 passed`.
- LOCAL-004 remains `passes: false` until the remaining operational scripts are classified and the DB-bound legacy tests are either run against the correct Docker DB endpoint or formally excluded as fixture-seed checks.
---

## 2026-05-10 - TABLE-006
- Completed XBRL taxonomy and PGC-XBRL remediation decision.
- Ran existing official/curated taxonomy loaders:
  - `docker compose run --rm worker-boe python xbrl_taxonomy.py` => `Inserted 34 taxonomy rows`.
  - `docker compose run --rm worker-boe python pgc_xbrl_mapping.py` => exit code `0`.
- Verified local counts:
  - `xbrl_taxonomy`: 34 rows.
  - `pgc_xbrl_mapping`: 42 rows.
  - `xbrl_company`, `xbrl_filing`, `xbrl_fact`: 0 rows.
- Reclassified `xbrl_company`, `xbrl_filing`, and `xbrl_fact` as `workflow_empty` because no official CNMV/ESEF filing ingestion source is configured locally. Fixture-first data is not acceptable production evidence.
- Evidence command:
  - `rtk powershell -NoProfile -Command "$env:PYTHONPATH='apps;apps/api'; python -m pytest apps/api/tests/test_xbrl.py apps/workers/tests/test_pgc_boe.py -q"` => `51 passed, 4 warnings`.
  - `python scripts\ralph\table_registry.py --gate scripts\ralph\table-remediation-registry.json` => exit code `2`, expected while 63 required blocker/derived-blocker tables remain empty.
- Updated `scripts/ralph/prd-local-table-remediation.json`: `TABLE-006` now has `passes: true`.
- **Learnings for future iterations:**
  - XBRL reference metadata can be loaded, but issuer filing/fact tables must stay empty until a real official filing source is implemented.
  - The next practical work is classification of event-only operational tables (`TABLE-007`) and then actual official-source remediation for remaining financial/regulatory blockers.
---

## 2026-05-10 - TABLE-001
- Completed authoritative local table remediation registry.
- Created `scripts/ralph/table_registry.py` to parse the remediation draft, query local Docker Postgres, generate the machine-readable registry, and verify it against the live DB table list.
- Created `scripts/ralph/table-remediation-registry.json` with all 163 public tables exactly once.
- Current registry summary:
  - total_tables: 163
  - populated: 45
  - blocker: 67
  - derived_blocker: 13
  - workflow_empty: 35
  - allowed_empty: 3
  - unclassified: 0
- Evidence commands:
  - `python scripts\ralph\table_registry.py --write scripts\ralph\table-remediation-registry.json` => `163,45,67,13,35,3,0`
  - `python scripts\ralph\table_registry.py --verify-json scripts\ralph\table-remediation-registry.json` => `OK: verified scripts\ralph\table-remediation-registry.json`
- Updated `scripts/ralph/prd-local-table-remediation.json`: `TABLE-001` now has `passes: true`.
- **Learnings for future iterations:**
  - The local DB currently has no unclassified empty table; the remaining problem is substantive remediation of 67 direct blockers and 13 derived blockers.
  - `workflow_empty` and `allowed_empty` are not production data shortcuts; `TABLE-002` must still gate their schema/RLS behavior.
---

## 2026-05-10 - LOCAL-002 / LOCAL-003 partial verification
- Fixed and verified `apps/workers/boe_modelos_worker.py` after local Docker rebuild exposed a real masked-password bug: `str(engine.url)` produced a password-redacted URL for secondary DB connections. Replaced with `engine.url.render_as_string(hide_password=False)`.
- Fixed `boe_modelos_worker.py` PDF retrieval to use the official BOE `<url_pdf>` from XML instead of fabricated `YYYY/01/01` PDF URLs.
- Restored a testable single-BOE-order sync path (`sync_orden_hac_to_db`, `OrdenSyncResult`) and verified the focused suite.
- Fixed and verified `apps/workers/aeat_irnr.py` against the current AEAT official IRNR page `https://sede.agenciatributaria.gob.es/Sede/impuestos-tasas/irnr.html`.
- IRNR worker now discovers and upserts official AEAT models `210, 211, 213, 216, 226, 228, 247` locally. Model `296` remains present from the broader AEAT model worker but is not listed on that IRNR index page.
- Rejected fake/unverified seed scripts as a way to satisfy row counts: `scripts/data/seed_sfdr.py`, `scripts/data/seed_crypto_asset.py`, `scripts/data/seed_irnr_rates.py`, and `scripts/data/seed_pgc_cuenta.py` are not acceptable evidence of real official-source data without provenance cleanup.
- Evidence commands:
  - `rtk powershell ... pytest apps/workers/tests/test_aeat_irnr.py apps/workers/tests/test_boe_modelos_worker.py apps/api/tests/test_source_manifest.py scripts/tests/test_maintenance_agents.py -q` => `30 passed`.
  - `docker compose build worker-boe` rebuilt worker image after host-code changes.
  - `docker compose run --rm worker-boe python boe_modelos_worker.py --run-once` fetched official BOE XML/PDF URLs and completed with sync_log statuses `ok/partial` and zero errors after the DB URL fix.
  - `docker compose run --rm worker-boe python aeat_irnr.py --run-once` discovered 7 official IRNR models and completed `7 upserted, 0 skipped, 0 deprecated`.
  - SQL: `aeat_modelo` now has IRNR `url_info` official AEAT URLs for `210, 211, 213, 216, 226, 228, 247`.
  - SQL row-count summary remains `45 populated tables, 118 empty tables`.
- Remaining blockers:
  - LOCAL-002 still fails: 118 tables are empty; several are compliance-domain tables and cannot be filled with fake seeds.
  - `irnr_withholding_rate` and `irnr_instruccion` remain empty; the IRNR model-discovery worker does not yet ingest official rate/instruction records into those tables.
  - `pgc_cuenta`, `sfdr_product`, `crypto_asset`, and multiple CSRD/DORA/EMIR/DAC/XBRL tables remain empty and need either official ingestion or explicit allowed-empty classification.
  - `boe_modelos_worker.py` still has partial runs for legal texts with zero parsed model casillas; this is now explicit in `sync_log` rather than silent.
- **Learnings for future iterations:**
  - A Docker run is not valid evidence after host edits unless the relevant image is rebuilt; this repo copies workers into the image rather than bind-mounting source.
  - Parallel evidence queries can race with worker completion; rerun SQL after the worker exits for final evidence.
---

## 2026-05-10 - LOCAL-001
- Completed local runtime inventory.
- Evidence commands:
  - `docker compose ps --all` shows local stack currently running `api`, `postgres`, `redis`, and `worker-boe`.
  - `python - <<yaml parser>> infra/deploy/docker-compose.prod.yml` found 42 Compose services, 15 `cron` profile services, and 14 continuous `worker-*` services.
  - `Get-ChildItem infra/deploy/systemd` found 17 `.timer` units and 4 `.service` units.
  - `docker exec esdata-postgres-1 psql ... pg_stat_user_tables` found 163 public tables.
  - Exact local row-count summary: 45 populated tables, 118 empty tables.
  - Local RLS summary: 0 public tables without RLS.
- Important blocker discovered for LOCAL-002:
  - The user's requirement "todas las tablas rellenas con datos reales" is not currently met locally: 118/163 tables have `count(*) = 0`.
- **Learnings for future iterations:**
  - `docker compose -f infra/deploy/docker-compose.prod.yml config --services` fails locally unless required prod env vars such as `DATABASE_URL` are supplied.
  - Use YAML parsing for inventory when Compose interpolation fails, then verify runtime with `docker compose ps --all`.
  - Local API container exposes `ESDATA_API_KEY=dev-key`, `MCP_API_KEY=dev-mcp-key`, and `APP_ENV=development`.
---

## 2026-05-10 - LOCAL-004
- Completed local script/tooling verification registry.
- Created `scripts/ralph/verify_scripts_local.py` and `scripts/ralph/script-verification-registry.json`.
- Registry summary: 206 scripts classified, 64 verified, 142 blocked at runtime by policy, 0 failures, 0 unclassified.
- Runtime blocks are intentional safety controls: mutative seed/backfill/schema/data scripts and deployment scripts are syntax-verified but not executed without dry-run support and official-source review.
- Fixed real Bash portability issue: `scripts/*.sh` files had CRLF line endings; normalized them and verified `bash -n` for all 5 shell scripts.
- Fixed prod env example drift:
  - Removed forbidden client-side `NEXT_PUBLIC_API_BASE_URL` from `infra/deploy/compose.env.example`.
  - Added missing prod Compose variables including `ESDATA_API_URL`, Grafana, worker interval, Hermes, and healthcheck ping vars.
  - Set `CADDY_EMAIL=admin@example.com` so `docker compose --env-file infra\deploy\compose.env.example -f infra\deploy\docker-compose.prod.yml --profile cron config --services` interpolates successfully.
- Fixed docs/tooling drift:
  - Added `DB_MAX_OVERFLOW` to `.env.example`.
  - Removed legacy `NEXT_PUBLIC_API_BASE_URL` from `docs/environment-variables.md`.
  - Documented missing workers in `docs/worker-inventory.md`: `document_decomposition.py`, `official_regulatory_references.py`, `pgc_boe.py`, `psd2_eba.py`.
  - Updated `scripts/maintenance/verify-doc-artifacts.py` to read UTF-16 BOM markdown instead of crashing.
  - Regenerated `docs/openapi-gpt.json` and `docs/openapi-gpt-3.0.json`.
- Evidence commands:
  - `python -m compileall -q scripts` => exit code `0`.
  - PowerShell parser gate => `powershell syntax OK: 1 files`.
  - Bash syntax gate => `bash syntax OK: 5 files`.
  - `python scripts\ralph\verify_scripts_local.py --write scripts\ralph\script-verification-registry.json --run-safe-probes` => `206,64,142,0`.
  - `python scripts\ralph\verify_scripts_local.py --gate scripts\ralph\script-verification-registry.json` => `OK: local script gate passed`.
  - Safe script pytest subset => `64 passed, 4 warnings`.
  - `python -m pytest scripts\tests\test_compose_env_example.py -q` => `3 passed`.
  - `python scripts\maintenance\verify-doc-contracts.py` => `docs contracts verified`.
  - `python scripts\ops\worker_scheduler_guard.py check --repo-root . --installed-unit infra\deploy\systemd\esdata-job@.service` => `WorkerSilent uses stale gauge: True`, `Installed unit has --no-deps: True`.
- Remaining non-LOCAL-004 blockers:
  - Full `scripts/tests` is not a valid local gate while host `127.0.0.1:5432` is owned by an external Postgres process instead of the Docker DB; legacy DB-bound seed tests hit that host port and fail independently of the Docker stack.
  - `scripts/maintenance/verify-doc-artifacts.py` now runs correctly but still reports real documentation debt: `[IMPLEMENTED]` claims that contradict roadmap `[PARTIAL]/[TARGET]`, duplicate headings, and long lines. This is documentation remediation, not script execution failure.
- **Learnings for future iterations:**
  - Script readiness must separate syntax/help/dry-run safety from runtime data mutation; seeds cannot be used to satisfy official-source table requirements.
  - Windows path interpolation can invalidate WSL/Bash checks; pass converted `/mnt/g/...` paths one file at a time for reliable shell syntax verification.
---

## 2026-05-10 - LOCAL-005
- Completed local MCP/API response accuracy gate for the highest-risk LIVA art. 90 path.
- Discovered and fixed a real blocking historical accuracy defect:
  - Before remediation, `/v1/legislacion/LIVA/articulos/90?vigente_en=2011-01-01` returned the original 15% text with `vigente_hasta=2012-07-15`.
  - Official BOE consolidated LIVA art. 90 lists the 2009 modification in force from 2010-07-01 and the 2012 modification in force from 2012-07-15; 2011 must resolve to 18%, not 15%.
  - `apps/workers/boe.py` now parses and persists every official `<version>` for each BOE block, while `parse_block_xml()` remains backward-compatible by returning the latest version.
  - `upsert_articulo()` now recalculates `vigente_hasta` from the next known `vigente_desde` for the article.
- Added `scripts/ralph/verify_mcp_api_local.py` and `scripts/ralph/mcp-api-local-results.json`.
- Gate summary: 10 checks, 10 passed, 0 failed.
- Validated:
  - `/health` and `/status` return 200.
  - Current LIVA art. 90 returns 21%, `vigente_desde=2012-07-15`, `vigente_hasta=null`, `boe_reference=BOE-A-1992-28740`, and BOE source URL `https://www.boe.es/buscar/act.php?id=BOE-A-1992-28740#a90`.
  - Historical LIVA art. 90 cases:
    - `vigente_en=1994-01-01` => 15%, `1993-01-01` to `1995-01-20`.
    - `vigente_en=2011-01-01` => 18%, `2010-07-01` to `2012-07-15`.
    - `vigente_en=2012-07-15` => 21%, `2012-07-15` onward.
  - `/v1/sources/freshness` returns 6 non-stale official source families: AEPD, BDE, CENDOJ, CNMV, EUR-Lex, SEPBLAC.
  - Missing article and unknown norma return explicit safe 404 JSON errors.
  - Query audit traceability exists for the checked `get_articulo` call with `verified=true` and retrieved chunks.
- Evidence commands:
  - Official BOE check: `https://www.boe.es/buscar/act.php?id=BOE-A-1992-28740#a90` shows art. 90 current 21%, BOE reference `BOE-A-1992-28740`, and version list including 2010-07-01 and 2012-07-15.
  - `docker compose run --rm -e BOE_LEGISLACION_NORMAS=LIVA -e BOE_ONLY_BLOCK_IDS=a90 -e WORKER_REQUEST_DELAY=0 worker-boe python boe.py --run-once` => `[run-once] Bloques: 1, Artículos: 1`.
  - SQL after remediation shows five LIVA art. 90 versions: `1993-01-01`, `1995-01-20`, `1996-01-01`, `2010-07-01`, `2012-07-15`, with correct `vigente_hasta` chain.
  - `python scripts\ralph\verify_mcp_api_local.py --base-url http://localhost:8001 --api-key dev-key --write scripts\ralph\mcp-api-local-results.json` => `10 passed, 0 failed`.
  - `python scripts\maintenance\mcp_validation_suite.py --read-only --base-url http://localhost:8001` => `ok: true`.
  - `pytest apps\workers\tests\test_boe.py apps\api\tests\test_mcp_audit.py apps\api\tests\test_source_manifest.py -q` => `45 passed, 1 warning`.
- **Learnings for future iterations:**
  - A current-answer test is insufficient in legal systems: boundary and historical `vigente_en` cases must be first-class gate checks.
  - BOE block XML already carries the historical versions needed for deterministic answers; dropping older versions creates silent legal inaccuracy.
---

## 2026-05-10 - LOCAL-006
- Completed the single local pre-VPS deployment gate.
- Created `scripts/ralph/local_full_gate.py` and `scripts/ralph/local-full-gate-results.json`.
- Gate summary: 5 checks, 5 passed, 0 failed.
- The gate covers:
  - `prd_status`: LOCAL-001 through LOCAL-005 all passing.
  - `table_registry_gate`: 163 tables classified with 0 blocker/derived-blocker/unclassified failures.
  - `worker_cron_artifacts`: 15/15 cron worker run-once artifacts passed, 0 failures, 0 timeouts.
  - `script_registry_gate`: 206 scripts classified, 64 verified, 142 blocked at runtime by policy, 0 failures.
  - `mcp_api_accuracy_gate`: live local API exactitude/freshness/audit checks, 10/10 passed.
- Evidence commands:
  - `python scripts\ralph\verify_scripts_local.py --write scripts\ralph\script-verification-registry.json --run-safe-probes` => `206,64,142,0`.
  - `python scripts\ralph\local_full_gate.py --base-url http://localhost:8001 --api-key dev-key` => `5 passed, 0 failed`.
- Important scope note:
  - This is a local pre-VPS gate. It validates the local Docker stack and artifacts. It does not claim VPS readiness because the user has not provided VPS access in this phase.
- **Learnings for future iterations:**
  - Long-running cron evidence should be artifact-gated after successful execution instead of rerun on every local gate; use explicit rerun commands only when changing worker behavior.
  - The final gate must execute the fast accuracy check live, because stale legal answers can reappear even when prior artifacts are green.
---

## 2026-05-10 - VPS maintenance agent assessment
- Reviewed MCP references and local maintenance-agent posture after user asked for current status and Hermes/OpenClaw-style options.
- Created `scripts/ralph/vps-maintenance-agent-assessment.md`.
- Current local gate remains PASS:
  - `python scripts\ralph\local_full_gate.py --base-url http://localhost:8001 --api-key dev-key` => `5 passed, 0 failed`.
- Docker local status:
  - `api` healthy on `8001`.
  - `postgres` healthy in Docker.
  - `redis` up.
  - `worker-boe` up.
- Hermes local evidence:
  - `python scripts\hermes_monitor.py --api-url http://localhost:8001 --no-restart` reached `/health` and `/status`.
  - It reported `29` workers from `/status` and `11` stale workers; this is a local-dev-stack signal, not a VPS production verdict, because only `worker-boe` runs continuously locally and cron workers are verified by artifacts.
  - DLQ host-side check failed because Windows host `localhost:5432` is an external PostgreSQL process, while the Docker DB is reachable via Compose; Docker SQL confirms `sync_dead_letter` has 2 rows.
- Decision:
  - Hermes-like monitor: useful on VPS, but deploy with read-only/restart-disabled permissions first.
  - Tester-healer: useful as read-only MCP/API validation loop; no autonomous legal-data mutation.
  - Regulatory awareness agent: useful as official-source digest only; no silent legal interpretation or record mutation.
---

## 2026-05-10 - External project support assessment
- Reviewed the external projects supplied by the user as possible support for empty-table remediation and MCP hardening.
- Created `scripts/ralph/external-project-support-assessment.md`.
- Decision rule:
  - These repositories may support parsers, schemas, validation, matching, MCP ergonomics, privacy controls, and test design.
  - They must not be treated as ground truth for Spanish tax/legal/compliance answers unless the persisted row points back to an official institutional source.
- Highest-value candidates:
  - Arelle for official CNMV/ESEF XBRL ingestion into `xbrl_company`, `xbrl_filing`, and `xbrl_fact`.
  - ESMA Data Py/direct ESMA registers for market-regulatory reference tables.
  - PyGLEIF/direct GLEIF for LEI/entity identity enrichment in `entity_identifiers` and `entity_aliases`.
  - OpenOwnership BODS for ownership schema validation.
  - OpenSanctions/Yente/Nomenklatura/FollowTheMoney for screening architecture and matching patterns, subject to license and official-list constraints.
  - AEAT MCP, MCP-BOE, Spanish Law MCP, and datos-gob-es-mcp for benchmark tests and MCP tool-design comparison.
- Explicitly rejected as direct production data sources:
  - Community Spanish tax/accounting tools, demo banking connectors, sample XBRL, fixture data, or LLM/legal-assistant outputs.
- Recommended Ralph backlog:
  - EXT-001: official XBRL filing ingestion with Arelle.
  - EXT-002: GLEIF entity identity ingestion.
  - EXT-003: ESMA register ingestion.
  - EXT-004: ownership schema alignment with BODS.
  - EXT-005: screening architecture with official-list-first ingestion.
  - EXT-006: MCP benchmark expansion from BOE/AEAT/legal MCP examples.
---

## 2026-05-10 - FINAL product readiness gate
- Created `scripts/ralph/prd-final-product-readiness.json`.
- Created `scripts/ralph/final_product_gate.py`.
- Created `docs/operations/final-product-readiness.md`.
- Hardened Alertmanager/Telegram deployment artifacts:
  - `infra/observability/alertmanager.yml` now uses `bot_token_file` for the Telegram token.
  - `TELEGRAM_CHAT_ID` is rendered at container startup because `prom/alertmanager:v0.28.1` does not support `chat_id_file`.
  - `infra/deploy/docker-compose.prod.yml` mounts `./secrets/alertmanager` read-only into Alertmanager.
  - `infra/deploy/secrets/alertmanager/README.md` documents the required token file.
  - `docs/operations/runbooks/grafana.md` documents secret setup and the manual Telegram alert test.
- Hardened Hermes logging:
  - `scripts/hermes_monitor.py` reconfigures stdout as UTF-8 with replacement to avoid Windows console encoding crashes when DB drivers return non-ASCII errors.
- Final local product gate result:
  - `python scripts\ralph\final_product_gate.py --base-url http://localhost:8001 --api-key dev-key` => `6 passed, 0 failed`.
- Gate coverage:
  - `local_full_gate`: `5/5 PASS`.
  - `compose_prod_config`: Docker Compose production config renders.
  - `alertmanager_telegram_config`: `prom/alertmanager:v0.28.1 amtool check-config` succeeds against rendered config with dummy token file.
  - `maintenance_agent_tests`: `20 passed`.
  - `hermes_read_only_probe`: runs inside Compose network with `AUTO_RESTART_ENABLED=false`.
  - `final_prd_status`: final PRD stories marked passing.
- Data readiness position:
  - Table registry remains `163` tables, `69` populated, `91` workflow-empty, `3` allowed-empty, `0` blockers, `0` unclassified.
  - Workflow-empty tables are intentionally not filled with fixtures/community data; they require user workflow data or a configured official upstream source.
- Scope note:
  - This is a local final product gate plus VPS artifact validation. Production readiness for `212.227.227.64` still requires running the documented smoke tests and Telegram delivery test on the VPS.
---
2026-05-10 13:10 Europe/Madrid | AEAT_CURRENT_1XX_2XX | COMPLETADO LOCAL | Nuevo worker `aeat_current_designs.py` ejecutado contra fuentes oficiales AEAT actuales: 74 enlaces oficiales 1XX/2XX descubiertos, 75 modelos activos con recurso oficial de diseno, 28 modelos con campos estructurados, 2.493 campos oficiales del modelo 100 extraidos desde diccionario `.properties`, 215 plazos del calendario AEAT 2026 activos. Integrado para VPS como `cron-aeat-current-daily` via systemd `esdata-aeat-current-daily.timer`.
