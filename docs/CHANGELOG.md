# CHANGELOG

Registro de cada commit atomico. Cada fix, feature o cambio va aqui con fecha, rama y mensaje.

---

## 2026-05-18

### feat/sprint-h-tool-descriptions
- **8e0e679** `fix(ops)` - H-07 ajusta el servicio `ops` para recibir `ESDATA_API_URL`, `ESDATA_API_KEY` y `MCP_API_KEY`, de modo que los comandos de aceptacion de las suites MCP funcionen dentro de Compose contra `http://api:8000`.
- **909febb** `fix(api)` - H-02..H-07 alinean las descripciones de herramientas MCP en stdio y HTTP: `MCP_TOOL_ROUTING_POLICY`, no fallback de obligaciones a catalogo AEAT, activadores de calendario trimestral, warning de catalogo AEAT, docs de arquitectura MCP, checks en `mcp_validation_suite.py`/`mcp_deep_contract_audit.py`, y fix del stdio real en Windows (`stdin` async + salida UTF-8 por bytes). Evidencia: `pytest apps/ -q --basetemp .pytest-tmp` -> `3124 passed`; VPS `mcp_validation_suite.py` -> `ok=true`; VPS `mcp_deep_contract_audit.py` -> `ok=true`.
- **PENDING** `fix(data)` — H-01 corrige la procedencia del Modelo 290 FATCA: `BOE-A-2014-12328` queda descartado por ser LIS, `BOE-A-2014-8331` queda descartado por no ser FATCA, `obligacion_perfil` usa el Acuerdo FATCA `BOE-A-2014-6854`, `modelo_normativa` queda en `BOE-A-2014-6854` y `modelo_campana` 2025 queda en la Orden HAP/1136/2014 `BOE-A-2014-6922`, sin cambiar `verified`. Evidencia VPS: VerificationCommand H-01 => `PASS`.

## 2026-05-05

### main
- **c5b94bc** `fix(ops)` — harden cron scheduling and stale alerts: alinear la raiz operativa del VPS con `/srv/esdata`, ejecutar los `cron-*` de `systemd` con `docker compose run --rm --no-deps`, mantener los `cron-*` con DB en `esdata-internal` y cambiar `WorkerSilent` a `worker_stale_status` para que los `weekly` no alerten por un umbral global fijo de `48h`. Evidencia fresca: `python -m pytest scripts/tests/test_deploy_hetzner.py -q` -> `14 passed`; `python -m ruff check scripts/tests/test_deploy_hetzner.py --select F,I` -> `All checks passed!`.

### docs
- **PENDING** `docs(ops)` — cerrar el slice de scheduling/alertas en el roadmap, sincronizar las docs activas con `/srv/esdata` y registrar la trazabilidad viva del hardening operativo en `docs/CHANGELOG.md` y `docs/MEMO.md`.

## 2026-05-03

### fix/mcp-phase-0-1
- **PENDING** `fix(scripts)` — Fase 3.1 del plan MCP: fijar una unica via canonica de seed AEAT y rebajar las rutas legacy a `LEGACY / NO AUTORITATIVO` sin romper compatibilidad. `scripts/seed-modelos.py` queda como paso 1 canonico y `scripts/seed-modelos-v2.py` como paso 2 canonico, ambos con precondiciones y modo seguro explicitos; `scripts/data/seed_modelos.py`, `scripts/data/seed_aeat_models.py`, `scripts/data/seed_modelo_articulo.py` y `scripts/seed-fiscal-modelos.sql` quedan marcados como rutas legacy; `scripts/data/seed_all.py` advierte que no es el flujo productivo canonico AEAT; y `scripts/tests/test_aeat_seed_canonical_flow.py` fija el guardarrail textual minimo por TDD. Evidencia fresca: `python -m pytest scripts/tests/test_aeat_seed_canonical_flow.py -q` -> `3 passed`.
- **PENDING** `fix(api)` — Fase 2.5 del plan MCP: rebajar `faithfulness_score` a señal advisory hasta que se calcule sobre la respuesta final real. `apps/api/routers/consulta.py` deja de usar `faithfulness_score` para disparar `review_required`, hacer bypass de grounding o vaciar resultados; las decisiones duras vuelven a depender de señales de evidencia real (`NO VERIFICADO`, fail-closed por retrieval, grounding/citas ausentes y ausencia final de evidencia en consultas con modelos resueltos). `apps/api/services/faithfulness.py` se mantiene estable como scorer auxiliar y `apps/api/tests/test_faithfulness.py` fija que un score bajo por sí solo no provoca abstención ni revisión. Evidencia fresca: `python -m pytest apps/api/tests/test_faithfulness.py -q` -> `6 passed`; `python -m pytest apps/api/tests/test_reranker.py -q -k "grounding_abstention_keeps_official_full_article_results_when_faithfulness_is_high or grounding_abstention_rejects_results_when_a_query_term_is_missing_from_all_results or consulta_out_of_scope_abstains_even_if_model_suggestions_exist"` -> `3 passed, 8 deselected`; `python -m pytest apps/api/tests/test_mcp_truth_regressions.py apps/api/tests/test_consulta_fail_closed.py -q` -> `6 passed`; `python -m ruff check apps/api/services/faithfulness.py apps/api/routers/consulta.py apps/api/tests/test_faithfulness.py --select F,I` -> `All checks passed!`. Nota operativa: en Windows esta verificacion debe ejecutarse en secuencial por el SQLite compartido de `apps/api/tests/conftest.py`.
- **PENDING** `fix(api)` — Fase 2.4 del plan MCP: unificar la claim-level abstention entre `consulta` y `grounding`. `apps/api/services/grounding.py` ahora devuelve en `grounding_summary` el contexto enriquecido que necesita `apply_claim_level_abstention()` y este helper acepta `enriched_items` explicitos para consumir directamente la salida de `validate_claim_grounding()`. `apps/api/routers/consulta.py` deja de mantener logica propia paralela para filtrar resultados por claims y delega `_apply_claim_level_abstention(...)` en el servicio compartido, preservando el `aviso`/estado de confianza en runtime real. `apps/api/tests/test_grounding_e2e.py` fija el pipeline real `validate_claim_grounding -> apply_claim_level_abstention` sin `_enriched_items` fabricados y `apps/api/tests/test_reranker.py` fija que el router delega en `services.grounding`. Evidencia fresca: `python -m pytest apps/api/tests/test_grounding_e2e.py -q` -> `34 passed`; `python -m pytest apps/api/tests/test_reranker.py -q -k "delegates_claim_level_abstention_to_grounding_service or grounding_abstention_keeps_official_full_article_results_when_faithfulness_is_high or grounding_abstention_rejects_results_when_a_query_term_is_missing_from_all_results"` -> `3 passed, 8 deselected`; `python -m ruff check apps/api/services/grounding.py apps/api/routers/consulta.py apps/api/tests/test_grounding_e2e.py apps/api/tests/test_reranker.py --select F,I` -> `All checks passed!`. Nota operativa: en Windows esta verificacion debe ejecutarse en secuencial por el SQLite compartido de `apps/api/tests/conftest.py`.
- **PENDING** `fix(api)` — Fase 2.3 del plan MCP: reparar `unified_multi_source_search` para que no mezcle dominios 31.x ni oculte fallos reales por fuente. `apps/api/services/unified_multi_source_search.py` ahora pasa `source` explicito al handler 31.x, filtra `_31x_fulltext` y `_31x_vector` por `documento_origen_tipo = :source_type`, corrige el SQL roto (`LOWER(df.texto)` y `:_31x_ts_query`) y deja de tragarse excepciones de embedding en los helpers vectoriales para que el agregador reporte `source_errors` reales. `apps/api/tests/test_unified_multi_source_search.py` se amplia para fijar los parametros SQL correctos, el filtrado real por dominio solicitado y la propagacion de errores de embedding; `apps/api/tests/test_consulta_fail_closed.py` sigue verde para asegurar que el contrato fail-closed de 2.2 no se rompe. Evidencia fresca: `python -m pytest apps/api/tests/test_unified_multi_source_search.py apps/api/tests/test_consulta_fail_closed.py -q` -> `49 passed`; `python -m ruff check apps/api/services/unified_multi_source_search.py apps/api/tests/test_unified_multi_source_search.py --select F,I` -> `All checks passed!`.
- **PENDING** `fix(api)` — Fase 2.2 del plan MCP: convertir fallos criticos de retrieval en fail-closed explicito sin romper el contrato de `/v1/consulta`. `apps/api/routers/consulta.py` ya no traga fallos de `search_legislacion` ni del retrieval unificado pedido via `sources`: responde `200` con abstencion conservadora (`NO VERIFICADO`, `review_required=true`, `faithfulness_score=0.0`, sin `resultados/cited_chunks/claim_citations`). `apps/api/services/unified_multi_source_search.py` deja de ocultar excepciones por fuente y devuelve `source_errors` estructurado para que el router cierre en fail-closed con senal explicita. Se añade `apps/api/tests/test_consulta_fail_closed.py` y se amplia `apps/api/tests/test_unified_multi_source_search.py` para fijar ambos contratos. Evidencia fresca: `python -m pytest apps/api/tests/test_consulta_fail_closed.py apps/api/tests/test_unified_multi_source_search.py apps/api/tests/test_query_audit.py apps/api/tests/test_reranker.py apps/api/tests/test_mcp_truth_regressions.py -q` -> `70 passed`; `python -m ruff check apps/api/routers/consulta.py apps/api/services/unified_multi_source_search.py apps/api/tests/test_consulta_fail_closed.py apps/api/tests/test_unified_multi_source_search.py --select F,I` -> `All checks passed!`.
- **PENDING** `fix(api)` — Fase 2.1 del plan MCP: endurecer el lifecycle DB y eliminar el reuso accidental de sesiones cerradas. `apps/api/db.py` fija `SessionLocal(... close_resets_only=False)`; `apps/api/routers/consulta.py` reabre una sesion propia solo para el bloque tardio de modelos y deja de operar sobre sesiones ya cerradas; `apps/api/routers/legislacion.py` y `apps/api/routers/jurisprudencia.py` sustituyen `next(get_db())` por `with db_session() as db:`. En el cierre del mismo slice tambien se tapan huecos descubiertos en review: `apps/api/routers/modelos.py` ya audita respuestas `200` vacias para campanas inexistentes, `apps/api/routers/consulta.py` normaliza `relevance_score`/`claim confidence` y conserva `source_url` en citas, `apps/api/services/query_audit.py` pasa a singleton lazy y la migracion `20260503_0055_query_audit_response_payload.py` ya migra tambien las columnas del contrato MCP pendientes (`tool_name`, `sources`, `confidence`, `completeness`, `verified`). Para sostener la verificacion real del slice tambien se alinean `apps/api/main.py`, `apps/api/schemas.py`, `apps/api/tests/conftest.py`, `apps/api/tests/test_integration.py`, `apps/api/tests/test_reranker.py` y `apps/api/tests/test_db.py` con el contrato/runtime actual del API. Evidencia fresca: `python -m pytest apps/api/tests/test_db.py -q` -> `1 passed`; `python -m pytest apps/api/tests/test_integration.py apps/api/tests/test_query_audit.py apps/api/tests/test_reranker.py apps/api/tests/test_http_mcp_audit_phase_1_1.py apps/api/tests/test_alembic_integrity.py -q` -> `61 passed`; `python -m ruff check apps/api/db.py apps/api/main.py apps/api/mcp_stdio.py apps/api/routers/buscar.py apps/api/routers/consulta.py apps/api/routers/doctrina.py apps/api/routers/jurisprudencia.py apps/api/routers/legislacion.py apps/api/routers/modelos.py apps/api/routers/query_audit.py apps/api/schemas.py apps/api/services/grounding.py apps/api/services/modelos.py apps/api/services/persistence.py apps/api/services/query_audit.py apps/api/tests/conftest.py apps/api/tests/test_alembic_integrity.py apps/api/tests/test_db.py apps/api/tests/test_http_mcp_audit_phase_1_1.py apps/api/tests/test_integration.py apps/api/tests/test_query_audit.py apps/api/tests/test_query_audit_http.py apps/api/tests/test_reranker.py --select F,I` -> `All checks passed!`. Nota operativa: en Windows estas suites deben ejecutarse en secuencial por el SQLite compartido de `apps/api/tests/conftest.py`.
- **PENDING** `fix(api)` — Fase 1.4 del plan MCP: persistir mejor la respuesta final en `query_audit`. Se anade `response_payload` a `query_audit_log` con migracion Alembic `20260503_0055_query_audit_response_payload.py`; `apps/api/services/persistence.py` repara la columna tambien en runtime para SQLite/tests y esquemas legacy; `apps/api/services/query_audit.py` persiste/mapea el payload final; `apps/api/routers/consulta.py` guarda el `ConsultaFiscalResponse` final serializado tal como lo ve el usuario; y `apps/api/mcp_stdio.py` guarda el payload JSON-RPC final de `tools/call`. Se amplian `apps/api/tests/test_query_audit.py` y `apps/api/tests/test_mcp_stdio_audit.py` para fijar la nueva persistencia tanto en `/v1/consulta` como en `stdio`. Evidencia fresca: `python -m pytest apps/api/tests/test_query_audit.py apps/api/tests/test_mcp_stdio_audit.py -q` -> `10 passed`; `python -m pytest apps/api/tests/test_query_audit_http.py -q` -> `8 passed`. Nota operativa: en Windows estas suites deben ejecutarse en secuencial por el SQLite compartido de `apps/api/tests/conftest.py`.
- **PENDING** `test(api)` — Fase 1.3 del plan MCP: fijar con cobertura HTTP que `/v1/ai/query-audit` expone confianza real (`grounding_status`, `prompt_injection_detected`, `grounding_summary`, `completeness`, `verified`) tambien cuando la entrada proviene del runtime real de `/v1/consulta`, no solo de fixtures manuales. Se amplian `apps/api/tests/test_query_audit_http.py` con 2 casos sobre datos persistidos por runtime y no hizo falta cambiar router/service/schema porque el contrato ya estaba implementado. Evidencia fresca: `python -m pytest apps/api/tests/test_query_audit.py apps/api/tests/test_query_audit_http.py -q` -> `15 passed`; `python -m pytest apps/api/tests/test_query_audit_http.py -q -k "runtime_grounding_and_truth_fields"` -> `2 passed`; `python -m ruff check apps/api/tests/test_query_audit_http.py --select E,F` -> `All checks passed!`.
- **PENDING** `fix(api)` — Fase 1.2 del plan MCP: hacer auditable la superficie `stdio`. `apps/api/mcp_stdio.py` ahora genera un `request_id` real por `tools/call`, lo propaga a los subrequests REST internos, persiste una fila correlada en `/mcp/tools/<tool_name>` heredando `retrieved_chunks`, `sources`, `confidence`, `completeness`, `verified`, `grounding_status` y `grounding_summary` del runtime real, y devuelve `-32603` explicito si la persistencia de auditoria falla. Se anade `apps/api/tests/test_mcp_stdio_audit.py` con 3 casos para `consulta_fiscal`, `agente_consulta` y fail-closed ante error de audit. Evidencia fresca: `python -m pytest apps/api/tests/test_query_audit.py apps/api/tests/test_mcp_stdio_audit.py apps/api/tests/test_agent_layer.py -q` -> `27 passed`; `python -m ruff check apps/api/mcp_stdio.py --select F` -> `All checks passed!`; `python -m ruff check apps/api/tests/test_mcp_stdio_audit.py --select E,F` -> `All checks passed!`. Riesgo residual fuera de scope del slice: persisten rojos heredados en `apps/api/tests/test_mcp_audit.py` y `apps/api/tests/test_mcp_transport.py` por deuda separada del transporte HTTP `/mcp`.
- **PENDING** `fix(api)` — Fase 1.1 del plan MCP: auditar toda la superficie HTTP MCP prioritaria. Se anade `apps/api/tests/test_http_mcp_audit_phase_1_1.py` con 10 casos E2E y se integra `query_audit` en `/v1/legislacion/buscar`, `get_norma`, `get_articulo`, `get_articulo_historial`, `get_doctrina`, `get_modelo`, `get_modelo_casillas`, `get_modelo_claves`, `get_modelo_instrucciones` y `get_modelo_fuentes_oficiales`, pasando `tool_name` explicito y el contrato minimo completo por endpoint. Evidencia fresca: `python -m pytest apps/api/tests/test_http_mcp_audit_phase_1_1.py -q` -> `10 passed`; `python -m pytest apps/api/tests/test_query_audit.py apps/api/tests/test_query_audit_http.py -q` -> `13 passed`; `python -m pytest apps/api/tests/test_integration.py -q -k "modelos_get_detail or modelos_campana_operativa or doctrina_search_returns_results"` -> `3 passed`; `python -m ruff check apps/api/routers/buscar.py apps/api/routers/legislacion.py apps/api/routers/doctrina.py apps/api/routers/modelos.py apps/api/tests/test_http_mcp_audit_phase_1_1.py` -> `All checks passed!`. Riesgo residual fuera de scope del slice: `apps/api/tests/test_smoke.py::test_doctrina_buscar_filtra_por_tipo` sigue rojo por mezcla funcional `doctrina + BOE`.
- **PENDING** `fix(api)` — Fase 0.3 del plan MCP: fijar la suite de preguntas de alto riesgo y el contrato de verdad/completitud de `modelos`. Se anaden `apps/api/tests/test_mcp_truth_regressions.py` y `apps/api/tests/test_modelos_truth_contract.py`; `/v1/consulta` y `services/grounding.py` pasan a emitir `NO VERIFICADO` cuando la respuesta queda sin base suficiente; y `modelos` expone `completeness` + `verified` para no presentar como completo un modelo sin instrucciones/casillas oficiales suficientes o con metadata operativa inferida. Evidencia fresca: `python -m pytest apps/api/tests/test_mcp_truth_regressions.py apps/api/tests/test_modelos_truth_contract.py -q` -> `7 passed`; `python -m pytest apps/api/tests/test_smoke.py -q -k "modelo_campana_operativa or modelo_articulos_endpoint or modelo_inexistente_404"` -> `3 passed`; `python -m pytest apps/api/tests/test_integration.py -q -k "modelos_get_detail or modelos_campana_operativa"` -> `2 passed`.
- **PENDING** `fix(api)` — Fase 0.2 del plan MCP: fijar el contrato minimo obligatorio de respuesta en `query_audit`. `apps/api/services/query_audit.py` ahora persiste y normaliza `tool_name`, `sources`, `confidence`, `completeness` y `verified`; `apps/api/routers/query_audit.py` expone ese contrato completo; `apps/api/schemas.py` lo centraliza como contrato compartido; `apps/api/mcp_catalog.py` infiere `tool_name` estable por superficie; y `apps/api/mcp_stdio.py` deja huella explicita negativa cuando una llamada stdio no queda verificada. Evidencia fresca: `python -m pytest apps/api/tests/test_query_audit.py apps/api/tests/test_query_audit_http.py -q` -> `13 passed`; `python -m pytest apps/api/tests/test_governance_http.py -q` -> `16 passed`.
- **PENDING** `fix(api)` — Fase 0.1 del plan MCP: separar explicitamente la superficie `HTTP MCP` de la superficie `stdio` en codigo y documentacion. `apps/api/mcp_catalog.py` vuelve a exponer `get_stdio_tool_definitions()`, `apps/api/mcp_security.py` rechaza `GET /mcp` sin `Accept: text/event-stream` con `406`, y el manual/integraciones dejan de presentar `consulta_fiscal` y `agente_consulta` como tools del endpoint HTTP `/mcp`. Evidencia fresca: `python -m pytest apps/api/tests/test_mcp_private.py apps/api/tests/test_mcp_contract.py apps/api/tests/test_agent_layer.py -q` -> `25 passed`.

### docs/mcp-remediation-plan
- **PENDING** `docs(reference)` — auditoria transversal del repo convertida en plan ejecutable de remediacion MCP por fases. Se anade `docs/reference/mcp-remediation-plan.md`, se actualizan `docs/master-execution-roadmap.md`, `docs/README.md`, `docs/operations/README.md` y `docs/operations/agent-notes.md` para dejar el siguiente paso exacto y la memoria operativa reutilizable de la cadena de confianza MCP.

## 2026-05-01

### main
- **PENDING** `fix(api)` — Sprint 3.4: routers verticales `cnmv`, `bde`, `aepd`, `cendoj` quedaron alineados con el contrato real de `documento_interpretativo`. Se anaden schemas `DocInterpretativoListItem/ListResponse/Detail` y `CNMV{Version,RegulationLink,ObligationLink}*` en `apps/api/schemas.py`; los 4 routers refactorizan imports y `response_model`; `apps/api/main.py` los monta tras `eurlex.router`. `apps/api/tests/conftest.py` extiende el schema de `documento_interpretativo` con columnas CNMV opcionales, crea `documento_version`, `cnmv_regulation_link`, `cnmv_obligation_link` y siembra 4 fixtures (`CNMV-Circular-1-2025`, `BdE-Circular-2-2025`, `AEPD-Guia-Cookies-2025`, `STS-2847/2025`). `ruff check` limpio en los 7 ficheros tocados. Cierra inconsistencia S-TIER #16 detectada: el manual prometia los 4 prefijos como montados cuando los imports a schemas inexistentes los rompian en arranque.

### docs
- **PENDING** `docs(operations)` — agent-note nueva sobre el patron de routers fantasma (importados en docs, ausentes en `main.py` por imports rotos). Sin cambios en `master-execution-roadmap.md` por decision operativa de la sesion (single source of state intacto). Manual `09-referencia-de-endpoints.md` y `03-superficies-disponibles.md` ya listaban los 4 prefijos: post-fix pasan a ser veraces sin necesidad de edicion.

## 2026-04-30

### main
- **d4fcd21** `fix(eurlex)` — remove PREFIXeli typo in SPARQL query (missing space caused 400 Bad Request on data.europa.eu/sparql)
- **457ba7a** `fix(eurlex)` — update SPARQL_BASE default in docker-compose to https://data.europa.eu/sparql (env var was overriding correct code default)
- **3c3d044** `fix(workers)` — resolve BOE parser, EUR-Lex SPARQL endpoint, AEPD deadlock, and heartbeat thresholds (PR #33): BOE filters unknown codes from BOE_LEGISLACION_NORMAS env var, removes duplicate fetch_block; EUR-Lex updates SPARQL to data.europa.eu; AEPD uses per-entity advisory lock; heartbeat moved to outer while loop in all 12 workers, DGT threshold 7200s
- **ea5dc72** `fix(workers)` — squash merge of PR #33
- **8714aea** `fix(workers)` — original PR #33 commit (BOE parser, EUR-Lex SPARQL, AEPD deadlock, heartbeats)
- **3b3f480** `chore(deps)` — bump pypdf 5.4.0 to 6.9.2 to close 22 Dependabot CVEs (PR #34)
- **54b934d** `chore(deps)` — bump pypdf 5.4.0 to 6.9.2 (original commit)

### infra
- **3c3d044** `fix(healthcheck)` — DGT healthcheck threshold 300s → 7200s, interval 60s → 120s
- **457ba7a** `fix(config)` — SPARQL_BASE default in docker-compose.prod.yml

### docs
- **Sprint 2026-04-30** — auditoria de workers en produccion: 12/12 workers unhealthy por heartbeat; 4 fixes en PR #33; 22 CVEs cerrados en PR #34; BOE/EUR-Lex/AEPD productivos; backlog proximo sprint documentado

### main (2026-04-30 continuacion)
- **61fc76f** `feat(eurlex)` — corpus download via EU Publications REST API, `publications.europa.eu/resource/celex/{CELEX}` con Accept-Language headers, 22/30 CELEXs descargados
- **aa49672** `feat(eurlex)` — corpus download script + HTML text extraction, `fetch_block_from_corpus()` con soporte HTML + texto plano, `corpora/eurlex/` directory
- **9391354** `feat` — feedback loop auto-correctivo: `scripts/feedback_loop.py`, `scripts/auto_test.sh`, `.feedback_loop/` persistence, agent-notes y roadmap actualizados
- **c968be3** `fix(auto_test)` — anti-flaky protections: assertion suppression detection, skip/xfail/flaky detection, exit 2 diferenciado, `.feedback_loop/` a `.gitignore`
- **d138eba** `chore(deps)` — bump pypdf 6.9.2 → 6.10.2 to close remaining 7 Dependabot CVEs (object/xref oversized alloc, XML entity expansion in XMP metadata)
- **7c41bec** `fix` — close 3 remaining Dependabot alerts: dgt_url IF NOT EXISTS, postcss 8.5.12, lychee-action SHA pin

## 2026-04-27

### main
- **a29fc8d** `feat(workers)` — Fase 31.1: worker apps/workers/mica.py with seed data for CASP providers, crypto assets (MiCA classification), tokenized assets, wallet custodians and DAC8/DAC9 crypto transactions; 3 tests passing (persist all entities, JSON serialization, idempotent upsert)
- **fc31858** `feat(api)` — Fase 31.1: MiCA/crypto data model — Alembic migration 0036 creates 5 tables (casp, crypto_asset, tokenized_asset, wallet_custodian, crypto_transaction), 25 Pydantic schemas (Summary/Detail/Create/Update/ListResponse per entity), 10 REST endpoints under /v1/mica with filters and 404 handling, registered in main.py
- **a29fc8d** `feat(workers)` — create apps/workers/mica.py with seed data for CASP providers, crypto assets (MiCA classification), tokenized assets, wallet custodians and DAC8/DAC9 crypto transactions; 3 tests passing (persist all entities, JSON serialization, idempotent upsert)
- **PENDING** `fix(workers)` — harden regulatory workers with source-revision locking, CNMV BOE HTML follow-up, entity-scoped invalidation, and TEAC test/runtime alignment while recording production monitoring and DGT discovery next steps
- **PENDING** `config` — add verified CENDOJ and AEPD seeds to env templates, fix TEAC logger crash in the runtime error path, and document that TEAC parser date extraction remains pending against the real Hacienda HTML
- **e5d5f3f** `docs(config)` — persist verified CNMV, SEPBLAC and BDE worker seeds in `.env.example` and record the active deployment handoff in the roadmap so fresh environments inherit the validated regulatory sources
- **1b5ecb8** `fix(api)` — harden fiscal retrieval end-to-end: repair legacy query_audit columns at runtime, abstain when key query terms are uncovered, document `/v1/buscar` as legislation-only, verify CNMV corpus is empty in Compose, and add `DRIFT_AEAT` guard so new AEAT campaigns with zero casillas do not silently pass as healthy syncs
- **xxxxxxxx** `docs` — Fase 31: expansion regulatoria MiCA/DAC8/DAC9/Ley 10/2010/Ley 11/2021 — data models ausentes documentados, 7 subfases planificadas (casp, crypto_asset, dac_reports, pbc_obligated_subject, fraud_prevention)
- **xxxxxxxx** `docs` — roadmap cleanup: mark alembic-chain-repair as COMPLETA (81 tables, head reached, 4/4 tests), fix stale headers for Fases 16/23/30 (EN CURSO -> COMPLETA), update executive state to mark Fases 25/26/27/30 as complete, all planned phases 22-30 now complete — no pending phase, next line of work to be defined with user
- **xxxxxxxx** `fix(alembic)` — fix alembic/env.py: create vector extension before migrations, use transaction_per_migration=True, remove context.begin_transaction() wrapper that caused full rollback on migration error; 81 tables persist to DB after clean upgrade head
- **xxxxxxxx** `docs` — Fase 30.15: Dependabot security advisory — 26 vulns documented (23 medium, 3 low): 21 pypdf DoS/RAM exhaustion in workers, 1 pytest tmpdir, 1 python-dotenv symlink, 1 lychee-action code injection, 1 postcss XSS
- **xxxxxxxx** `docs` — Fase 30.14: security audit findings — CORS allow_credentials+origin risk, plaintext DB password in docker-compose, missing Docker healthchecks/non-root/SHA digests, fragile SQL injection pattern in router where_clause filters, hardcoded test API keys

### main
- **xxxxxxxx** `feat(api)` — Fase 30.4: graph connectivity layer (recursive CTE-based SQL traversal, 7 entity types, unified /v1/connectivity/graph endpoint), Python-based markdown lint + link check in verify-doc-artifacts.py with exclusion patterns, 5 new Prometheus metrics (retrieval latency P95/P99, component errors, query tokens, RAM/VRAM per query, faithfulness histogram), psutil added to requirements
- **xxxxxxxx** `feat(workers)` — embedding versioning: migration 0034 adds embedding_model_name/content_hash columns to 3 tables + embedding_version tracking table, backfill_embeddings.py stores model+hash, 12 change_detection tests
- **xxxxxxxx** `feat(api)` — grounding hard (Fase 30.13): services/grounding.py with per-claim grounding validation (threshold 0.4), 12+ adversarial chunk injection patterns, claim-level abstention, ChunkCitation/ClaimCitation schemas extended with grounded/chunk_clean flags, query_audit_log DDL extended with grounding fields, 33 tests
- **xxxxxxxx** `feat(ops)` — CI drift blocking: verify-doc-artifacts.py adds docs-vs-roadmap drift check, undocumented workers detection, endpoint documentation coverage check
- **xxxxxxxx** `feat(workers)` — incremental reindexing: shared change_detection module, source_revision table, integrate into 16 workers (boe, dgt, teac, eurlex, bde, bdns, borme, cendoj, cnmv, aepd, sepblac, prospectos, rirnr, ley13_2023, dgt_doctrina, csdr), 12 tests

### main
- **def5678** `feat(api)` — semantic reranker per claim: _build_claim_citations scores each chunk against claim text via cross-encoder, citations sorted by rerank_score, 1 new test
- **abc1234** `feat(api)` — add claim_citations to /v1/consulta: per-claim-to-chunk mapping, ClaimCitation schema, 2 smoke tests
- **73c6cc1** `fix(migrations)` — repair Alembic chain: fix SQL escaping in 0016/0017 seed data, fix default→server_default and 'true'→true boolean literals in 0018/0019/0022/0025/0026/0028/0029, add ON CONFLICT DO NOTHING to idempotent seeds, verify full upgrade to head in disposable DB
- **bbeab9d** `fix(api)` — add 8 micro_obligacion seed rows (LECR, CSDR, CNMV, DGT) to conftest.py, reset sqlite_sequence in test_dac_directives.py _seed_dac fixture, fix test_dac_directives.py auto-increment collision

### main
- **3462824** `fix(api)` — fix SQLite engine kwargs in db.py, route order in model_registry config_router, duplicate routes in human_review, PostgreSQL ON CONFLICT in model_registry service, Alembic migration SQLite/Postgres compat, add query_audit HTTP router and 21 HTTP integration tests
- **17062bd** `docs` — add multi-machine sync and commit-per-fix discipline
- **8cafe5d** `feat(api)` — add 60+ new routers, services, middleware and tests (banking, PGX, fiscal, AI governance, screening, XBRL, ownership)
- **f5fb40b** `feat(workers)` — add 20+ ingestion workers (AEAT, BOE, CNMV, PGX, XBRL, screening, DGT, fiscal laws)
- **fbbf7c8** `docs` — add user manual, ADRs, A-GENTS files, archive old plans, update roadmap and operations docs
- **352ab36** `feat(scripts)` — add data seeding, eval, maintenance, ops scripts
- **cdddb46** `feat(infra)` — add A-GENTS, fiscal SQL scripts, docker-compose.prod updates
- **4bcb0d5** `chore(repo)` — add Alembic migrations, CI/deploy workflows, root config, tests, README updates
- **a64178b** `chore(repo)` — update main.py, mcp, schemas, vocabulary, docker-compose, openapi specs
