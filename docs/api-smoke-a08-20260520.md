# API Smoke A-08 - 2026-05-20

## Scope

A-08 ran against the VPS API at `root@212.227.227.64`, `http://localhost:8000`.

The smoke covers the PRD endpoints plus two historical regressions:

- `/health`
- `GET /v1/legislacion/LIVA/articulos/1`
- `GET /v1/buscar?q=IVA&norma=LIVA`
- MCP HTTP `tools/list`
- `POST /v1/ai/consulta`
- `GET /v1/eurlex/MIFID2_2014_65`

The API exposes canonical routes under `/v1`; the MCP catalog is exposed through JSON-RPC at `/mcp`.

## Results

```json
{
  "health": {
    "status": "ok",
    "database": "ok"
  },
  "liva_art1": {
    "numero": "1",
    "boe_reference": "BOE-A-1992-28740",
    "source_url": "https://www.boe.es/buscar/act.php?id=BOE-A-1992-28740#a1",
    "texto_len": 402
  },
  "search_total": 10,
  "ai_consulta": {
    "status": "matched",
    "safe_to_answer": true,
    "evidence_notice": null
  },
  "eurlex": {
    "referencia": "32014L0065",
    "articulos_total": 92,
    "texto_len": 393434
  },
  "eurlex_legacy": {
    "http_status": "404",
    "detail": {
      "error": "Documento EUR-Lex no encontrado"
    }
  },
  "mcp": {
    "handshake_status": "400",
    "tools_count": 77
  }
}
```

## Endpoint Details

| check | result | notes |
|---|---:|---|
| `/health` | `200` | `status=ok`, `database=ok`. |
| `/v1/legislacion/LIVA/articulos/1` | `200` | Text is non-empty; `boe_reference=BOE-A-1992-28740`; source URL points to BOE. |
| `/v1/buscar?q=IVA&norma=LIVA` | `200` | `10` results returned. |
| `/v1/ai/consulta` | `200` | I-00 regression remains fixed; payload includes `status`, `safe_to_answer`, and `evidence_notice`. For the tested query, `evidence_notice=null` because the answer is matched/safe. |
| `/v1/eurlex/32014L0065` | `200` | Canonical CELEX resolves with `92` articles and non-empty text. |
| `/v1/eurlex/MIFID2_2014_65` | `404` | Explicit not-found payload; no `500`. This is acceptable because the canonical stored code is `32014L0065`. |
| MCP `tools/list` via `/mcp` JSON-RPC | `200` | `77` tools returned. |

## MCP Transport Caveat

The initial `GET /mcp` handshake currently returns:

```text
HTTP 400
Bad Request: Missing session ID
```

It still issues an `mcp-session-id` response header. Using that session id:

- JSON-RPC `initialize` returns `200`
- JSON-RPC `tools/list` returns `200`
- tool count is `77`

This means the tool catalog is healthy, but A-11/A-final should track whether the MCP transport should return `200` on the initial session negotiation, as older tests expected.

## Conclusion

A-08 passes with one transport caveat:

- REST API smoke: PASS
- I-00 `POST /v1/ai/consulta` regression: PASS
- A-04b EUR-Lex legacy alias no-500 behavior: PASS
- MCP tool catalog: PASS
- MCP initial GET status: caveat, `400` with valid session id and successful follow-up JSON-RPC calls.
