# [DEPRECATED] Railway deploy disabled
#
# Production deploy is Docker Compose on the VPS. This file is intentionally
# inert so an old Railway deployment cannot run on push to main.
# Historical copy: docs/archive/workflows/deploy-railway.md
# Active deploy workflow: .github/workflows/deploy-hetzner.yml

name: Deploy Railway [DISABLED]

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled workflow
        run: |
          echo "Railway deployment is disabled."
          echo "Use the Docker Compose VPS deployment workflow instead."
